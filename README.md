# Button Component Generator

A Vite + React tool for visually designing button styles, previewing hover and disabled states, and exporting ready-to-copy HTML/CSS or React/CSS code.

## Run

```bash
npm install
npm run dev
```
